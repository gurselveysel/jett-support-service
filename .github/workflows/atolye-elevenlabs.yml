name: Atolye ElevenLabs R12 Batch03
run-name: Atolye R12 Batch03 - 8 unique questions - included credit
on:
  push:
    branches: [atolye-elevenlabs-20260921]
    paths: [.github/workflows/atolye-elevenlabs.yml]
  workflow_dispatch:
permissions: {}
concurrency:
  group: atolye-elevenlabs-production
  cancel-in-progress: false
jobs:
  speech:
    if: github.ref == 'refs/heads/atolye-elevenlabs-20260921'
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Ensure audio verification tools
        run: |
          if ! command -v ffprobe >/dev/null || ! command -v ffmpeg >/dev/null; then
            sudo apt-get update -qq
            sudo apt-get install --no-install-recommends -y ffmpeg
          fi
      - name: Verify quota and produce 8 real narrations with provider timestamps
        shell: python
        env:
          ELEVENLABS_API_KEY: ${{ secrets.ELEVENLABS_API_KEY }}
        run: |
          import json, pathlib
          class ProductionStop(Exception): pass
          def produce(item):
              import os, sys, json, pathlib, urllib.request, urllib.error, base64, hashlib, re, math
              out = pathlib.Path('atolye-r12-batch03')/item['id']
              out.mkdir(parents=True,exist_ok=True)
              voice = 'tZYpRZkmCNQcfmtDbhUg'
              model_id = 'eleven_v3'
              report = {'pair':item['pair'],
                        'provider':'elevenlabs','voice_id':voice,'model_id':model_id,
                        'commit':os.environ.get('GITHUB_SHA'), 'run_id':os.environ.get('GITHUB_RUN_ID'),
                        'credential_present':False,'api_called':False,'tts_called':False,
                        'audio_generated':False,'fallback_used':False,'video_completed':False,
                        'auditory_review':False}
              def save_report(status, **extra):
                  report.update(status=status, **extra)
                  (out/'report.json').write_text(json.dumps(report, ensure_ascii=False, indent=2), encoding='utf-8')
                  print('ATOLYE_RESULT '+json.dumps(report,ensure_ascii=False))
              def stop(status, **extra):
                  save_report(status, **extra)
                  raise ProductionStop(status)
              key = os.environ.get('ELEVENLABS_API_KEY','').strip()
              if not key:
                  stop('blocked_missing_credential', required_secret='ELEVENLABS_API_KEY')
              report['credential_present'] = True
              # A rerun must never unknowingly spend credits twice. Use a fresh dispatch only after inspecting artifacts.
              if int(os.environ.get('GITHUB_RUN_ATTEMPT','1')) > 1:
                  stop('blocked_automatic_audio_regeneration', action='Inspect and reuse existing audio artifacts; fresh dispatch only if no audio was generated.')
              text=item['narration']
              class NoRedirect(urllib.request.HTTPRedirectHandler):
                  def redirect_request(self, req, fp, code, msg, headers, newurl):
                      return None
              opener = urllib.request.build_opener(NoRedirect())
              def call(path, payload=None):
                  report['api_called'] = True
                  req = urllib.request.Request('https://api.elevenlabs.io'+path,
                      headers={'xi-api-key':key,'Content-Type':'application/json'},
                      data=None if payload is None else json.dumps(payload,ensure_ascii=False).encode('utf-8'),
                      method='GET' if payload is None else 'POST')
                  try:
                      with opener.open(req, timeout=180) as response:
                          return json.loads(response.read(24000000)), response.headers.get('request-id')
                  except urllib.error.HTTPError as error:
                      try:
                          detail=json.loads(error.read(4000)).get('detail',{})
                          provider_code=detail.get('status') if isinstance(detail,dict) else None
                          if not isinstance(provider_code,str) or not re.fullmatch(r'[A-Za-z0-9_.-]{1,100}',provider_code) or key in provider_code:
                              provider_code=None
                      except Exception:
                          provider_code=None
                      stop('provider_error', http_status=error.code, provider_code=provider_code,
                           operation='tts' if payload is not None else path)
                  except Exception as error:
                      stop('transport_or_response_error', error_type=type(error).__name__,
                           tts_outcome_uncertain=payload is not None)
              voice_info,_=call('/v1/voices/'+voice)
              if voice_info.get('voice_id') != voice:
                  stop('blocked_voice_mismatch')
              report['voice_accessible'] = True
              models,_=call('/v1/models')
              selected=next((m for m in models if m.get('model_id')==model_id),None)
              if not selected or not selected.get('can_do_text_to_speech'):
                  stop('blocked_model_unavailable')
              report['model_accessible'] = True
              subscription,_=call('/v1/user/subscription')
              used=subscription.get('character_count')
              limit=subscription.get('character_limit')
              if type(used) is not int or type(limit) is not int or used<0 or limit<0:
                  stop('blocked_quota_not_verifiable')
              # No billing settings are changed. Stop unless paid overage is known to be disabled.
              extension=subscription.get('max_credit_limit_extension')
              overage_disabled=(type(extension) is int and extension==0) or subscription.get('can_extend_character_limit') is False
              if not overage_disabled:
                  stop('blocked_paid_overage_not_disabled', action='No audio request made; billing settings were not changed.')
              rates=selected.get('model_rates') or {}
              cost_factor=rates.get('character_cost_multiplier',selected.get('token_cost_factor'))
              if not isinstance(cost_factor,(int,float)) or isinstance(cost_factor,bool) or not math.isfinite(cost_factor) or cost_factor<=0:
                  stop('blocked_model_cost_not_verifiable')
              required=math.ceil(len(text)*max(1.0,float(cost_factor)))
              remaining=limit-used
              report.update(narration_characters=len(text), required_credits_conservative=required,
                            quota_checked=True, paid_overage_disabled=True, remaining_included_credits=remaining)
              if remaining < required:
                  stop('blocked_insufficient_included_credits', required_credits=required, remaining_included_credits=max(0,remaining))
              maximum=selected.get('maximum_text_length_per_request')
              tier_limit=selected.get('max_characters_request_free_user' if subscription.get('tier')=='free' else 'max_characters_request_subscribed_user')
              limits=[x for x in [maximum,tier_limit] if isinstance(x,int) and x>0]
              if limits and len(text)>min(limits):
                  stop('blocked_text_exceeds_model_limit', model_character_limit=min(limits))
              payload={'text':text,'model_id':model_id,'language_code':'tr',
                       'voice_settings':{'stability':0.5,'speed':0.94}}
              (out/'narration.txt').write_text(text,encoding='utf-8')
              report['narration_sha256']=hashlib.sha256(text.encode('utf-8')).hexdigest()
              report['tts_called']=True
              save_report('tts_request_started')
              result,request_id=call('/v1/text-to-speech/'+voice+'/with-timestamps?output_format=mp3_44100_128',payload)
              try:
                  audio=base64.b64decode(result['audio_base64'],validate=True)
                  if len(audio)<10000:
                      stop('invalid_audio_size')
                  (out/'speech.mp3').write_bytes(audio)
                  report.update(audio_generated=True, bytes=len(audio), sha256=hashlib.sha256(audio).hexdigest())
                  (out/'alignment.json').write_text(json.dumps({'alignment':result.get('alignment'),'normalized_alignment':result.get('normalized_alignment')},ensure_ascii=False,indent=2),encoding='utf-8')
                  alignment=result.get('normalized_alignment') or result.get('alignment')
                  if not alignment:
                      stop('audio_saved_but_alignment_missing', action='Reuse saved audio; do not regenerate for drawing.')
                  chars=alignment['characters']
                  starts=alignment['character_start_times_seconds']
                  ends=alignment['character_end_times_seconds']
                  assert len(chars)==len(starts)==len(ends)>0
                  assert all(isinstance(c,str) and len(c)==1 for c in chars)
                  assert all(math.isfinite(float(s)) and math.isfinite(float(e)) and 0<=float(s)<=float(e) for s,e in zip(starts,ends))
                  assert all(float(starts[i])<=float(starts[i+1]) for i in range(len(starts)-1))
                  joined=''.join(chars)
                  def segments(pattern):
                      return [{'text':m.group().strip(),'start':starts[m.start()],'end':ends[m.end()-1]} for m in re.finditer(pattern,joined) if m.group().strip()]
                  words=segments(r'\S+')
                  phrases=segments(r'[^.!?\n]+[.!?]?')
                  speech={'provider':'elevenlabs','voice_id':voice,'model_id':model_id,
                          'duration_alignment':max(ends),'word_timestamps':words,'phrase_timestamps':phrases,
                          'text':text,'request_id':request_id,'timing_source':'provider_character_alignment'}
                  (out/'speech.json').write_text(json.dumps(speech,ensure_ascii=False,indent=2),encoding='utf-8')
                  save_report('audio_saved_pending_decode', word_count=len(words),phrase_count=len(phrases),
                              last_alignment_second=max(ends), character_alignment_validated=True)
              except Exception as error:
                  stop('audio_processing_error',error_type=type(error).__name__,action='Inspect saved files before any new TTS request.')
              import subprocess
              probe=json.loads(subprocess.check_output(['ffprobe','-v','error','-show_streams','-show_format','-of','json',str(out/'speech.mp3')]))
              duration=float(probe['format']['duration'])
              assert math.isfinite(duration) and duration>1
              assert any(s['codec_type']=='audio' for s in probe['streams'])
              assert report['last_alignment_second']<=duration+0.5
              subprocess.run(['ffmpeg','-v','error','-xerror','-i',str(out/'speech.mp3'),'-f','null','-'],check=True,capture_output=True)
              save_report('audio_generated_and_decoded',duration_seconds=duration,audio_decode_pass=True,auditory_review=False)

          items=[{'id': 'A-Temel_Matematik-13', 'pair': ['A-Temel_Matematik-13', 'B-Temel_Matematik-17'], 'narration': 'Akış şemasındaki toplam değişkeni sıfırdan başlıyor. Boş bırakılan yerde, toplama hangi sayıları hangi sırayla ekleyeceğimizi belirleyen döngü bulunmalı.\n\nSoru, ikiden kırka kadar olan çift sayıların toplamını istiyor. İlk sayı iki, son sayı kırk. Bir çift sayıdan sonrakine geçerken iki ekleriz. Dolayısıyla döngünün başlangıcı iki, bitişi kırk, artış miktarı iki olmalıdır.\n\nB seçeneğindeki döngü bu üç bilgiyi doğru verir. Birer artırsaydık araya tek sayılar da girerdi. Birden başlasaydık yine istenen çift sayı dizisini elde edemezdik.\n\nKontrol edelim: iki, dört, altı diye devam edip kırkta biten yirmi terim var. Toplamları, yirmi çarpı iki artı kırk, bölü iki; yani dört yüz yirmidir. B seçeneğini işaretliyoruz.'}, {'id': 'A-Temel_Matematik-14', 'pair': ['A-Temel_Matematik-14', 'B-Temel_Matematik-16'], 'narration': 'Döngü ikiyle başlıyor, yetmiş dörde kadar üçer artarak ilerliyor. İ değişkeninin aldığı ilk değerler iki, beş ve sekizdir. Her turda, bu sayının üçe bölümünden kalan iki mi diye bakılıyor.\n\nİlk sayı ikinin kalanı iki. Üç eklemek kalanı değiştirmez. Bu yüzden döngüdeki bütün değerler koşulu sağlar. Sayaç her turda bir artar.\n\nKaç tur olduğunu bulalım. Son terimden ilk terimi çıkarıp artış miktarına böleriz: yetmiş dört eksi iki, bölü üç, yirmi dört eder. Bu sayı aralıkların sayısıdır. Başlangıç terimini de saymak için bir ekleriz.\n\nYirmi dört artı bir, yirmi beş. Sayaç sıfırdan başladığına göre ekrana yirmi beş yazılır. E seçeneğini işaretliyoruz.'}, {'id': 'A-Turkce-03', 'pair': ['A-Turkce-03', 'B-Turkce-14'], 'narration': 'Sözcük gruplarını yanlarındaki açıklamalarla karşılaştıralım. Özgün sözcüğü orijinal anlamında, ihtiras ise tutku anlamında kullanılmış. Bu iki eşleştirme uygundur.\n\nÜçüncü gruptaki içten içe, duygu veya düşüncenin dışarıya belli edilmeden, gizlice yaşanmasını anlatır. İyiden iyiye ise iyice, oldukça veya bütünüyle anlamına gelir.\n\nBirinde gizlilik, diğerinde derece ve yoğunluk anlatılıyor. Bu iki söz birbirinin açıklaması olamaz. Meta, mal anlamını; reaksiyon göstermek de tepki vermek anlamını karşılar. Uyuşmayan eşleştirme üçüncüdür. C seçeneğini işaretliyoruz.'}, {'id': 'A-Turkce-04', 'pair': ['A-Turkce-04', 'B-Turkce-13'], 'narration': 'Cümlede sanatın hangi işlevleri vurgulanıyor, onları ayıralım. Sanatın yalnız insanı avutup büyüleyen bir güç olarak görülmesine karşı çıkılıyor. İnsanı uyarması ve harekete geçirmesi öne çıkarılıyor.\n\nHarekete geçirmek, eylem yönüdür. Ufkumuzu genişletmek ve yaşadıklarımıza daha anlamlı bakmamızı sağlamak ise düşünce yönünü anlatır. Yani sanatın hem düşünce hem eylem bakımından işlevi vardır.\n\nA seçeneği zevk vermenin ötesinde düşünce ve eylem işlevlerini birlikte söylüyor. Diğer seçeneklerdeki evrensel dil, eserin değerini ölçme ya da yeni dünyaların kapısını açma anlatımları bu iki vurguyu birlikte karşılamıyor. A seçeneğini işaretliyoruz.'}, {'id': 'A-Sosyal_Bilimler-02', 'pair': ['A-Sosyal_Bilimler-02', 'B-Sosyal_Bilimler-03'], 'narration': 'Sorudaki anahtar bilgi, geçmişte ve günümüzde konuşulan dillerin gelişiminin incelenmesi. Dillerin tarihini, yapısını ve gelişimini inceleyen, bu yolla tarihe yardımcı olan alan filolojidir.\n\nBenzer görünen seçeneği ayıralım: paleografya eski yazıların biçimlerini inceler. Dilin gelişimi ile yazı biçimini birbirine karıştırmıyoruz. Kronoloji olayların zamanını ve sırasını; heraldik ise armaları ele alır.\n\nBurada sorulan yazının şekli veya olayların tarihi değil, diller ve gelişimleridir. Bu nedenle filolojiyi veren E seçeneğini işaretliyoruz.'}, {'id': 'A-Sosyal_Bilimler-07', 'pair': ['A-Sosyal_Bilimler-07', 'B-Sosyal_Bilimler-09'], 'narration': 'Pasifik Ateş Çemberi, Büyük Okyanusu çevreleyen levha sınırları boyunca uzanır. Haritadaki yerlerin hangi okyanusun kenarında olduğuna bakalım.\n\nBirinci yer Kuzey Amerikanın batı kıyısındadır. Bu kıyı Pasifik Okyanusuna bakar. Dördüncü yer de Pasifikin batı kenarındaki ada kuşağında bulunur. Her ikisi de Ateş Çemberi üzerindedir.\n\nİkinci yer İzlanda çevresinde, Atlas Okyanusundadır. Üçüncü yer ise Akdeniz çevresindedir. Aktif volkan bulunması, bir yerin mutlaka Pasifik Ateş Çemberinde olması demek değildir.\n\nBu yüzden birinci ve dördüncü yerleri birlikte seçeriz. C seçeneğini işaretliyoruz.'}, {'id': 'A-Fen_Bilimleri-03', 'pair': ['A-Fen_Bilimleri-03', 'B-Fen_Bilimleri-05'], 'narration': 'Üç barometre aynı ortamda olduğu için aynı açık hava basıncını ölçüyor. Boruların üst kısmında boşluk var. Sıvı sütununun basıncı, sıvının özkütlesi, yerçekimi ivmesi ve sütun yüksekliğinin çarpımıdır.\n\nBasınç ve yerçekimi aynı olduğuna göre özkütle ile yükseklik ters orantılıdır. Daha kısa bir sıvı sütunu aynı basıncı dengeliyorsa, o sıvının özkütlesi daha büyüktür.\n\nYükseklikleri sıralayalım. Be sütunu altmış dört santimetreyle en kısa. Ce sütunu seksen santimetre. A sütunu ise yüz on santimetreyle en uzun.\n\nÖzkütle sırası bunun tersidir: be, ce, a. Be sıvısının özkütlesi ceden, ceninki de a sıvısından büyüktür. A seçeneğini işaretliyoruz.'}, {'id': 'A-Fen_Bilimleri-09', 'pair': ['A-Fen_Bilimleri-09', 'B-Fen_Bilimleri-12'], 'narration': 'Üç kapta da aynı tuzun sulu çözeltisi var. Aynı koşullarda kaynama sıcaklıkları eşit olduğuna göre çözeltilerin bileşim derişimleri aynıdır. Aynı tuz söz konusu olduğu için iyon derişimleri de eşittir. Birinci yargı doğrudur.\n\nÜçüncü kapta yarım mol tuz, yarım litre çözeltide bulunuyor. Litre başına bir mol düşer. Birinci kaptaki bir mol tuz için bir litre, ikinci kaptaki iki mol için iki litre çözelti gerekir. Yani iks bin, ye iki bin mililitredir. İkinci yargı da doğrudur.\n\nHer kaba bir litre su eklemek, hacimleri aynı oranda artırmaz. Sorunun hacimleri toplama kabulüyle yeni hacimler iki litre, üç litre ve bir buçuk litre olur. Yalnız ilk kabın hacmi iki katına çıkar; üç çözeltinin birden derişimi yarıya inmez.\n\nÜçüncü yargı yanlış. Birinci ve ikinci yargıları birlikte veren B seçeneğini işaretliyoruz.'}]
          for item in items:
              try:
                  produce(item)
              except ProductionStop:
                  raise SystemExit(2)
              except Exception as error:
                  print('BATCH_STOP '+json.dumps({'id':item['id'],'error_type':type(error).__name__,'action':'Inspect saved artifacts before any new TTS request.'}))
                  raise SystemExit(3)

      - name: Preserve all generated audio and timestamps even after partial failure
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: atolye-r12-batch03-${{ github.run_id }}
          path: atolye-r12-batch03/
          if-no-files-found: warn
          retention-days: 30
